{
  "name": "ann-symbol-recognition",
  "version": "1.2.0",
  "description": "AI algorithm to recognize any letter or number.",
  "main": "index.js",
  "scripts": {
    "start": "live-server --port=3003 --host=localhost",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "Samuel Kedziora <samukedo@gmail.com>",
  "private": false,
  "license": "ISC",
  "dependencies": {
    "live-server": "^1.2.1"
  }
}
